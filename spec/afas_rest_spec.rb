require "spec_helper"

RSpec.describe AfasRest do
  it "has a version number" do
    expect(AfasRest::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
